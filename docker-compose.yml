services:
  postgres:
    image: postgres:16
    container_name: essenciacriativa-db
    environment:
      POSTGRES_DB: essenciacriativa
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: